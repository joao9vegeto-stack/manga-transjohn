# Font Directory

Place your `WildWorlds.ttf` font file in this directory.

The typesetting service expects the font at:
```
/app/fonts/WildWorlds.ttf
```

If the font is not found, the application will fall back to the system default font.

## Note
The WildWorlds font is not included in this repository. You must provide your own font file.
